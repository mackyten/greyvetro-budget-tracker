<?xml version="1.0" encoding="utf-8"?>
<!-- Dark-mode splash: exact match to AppPalette.dark.bg (lib/core/design_tokens.dart)
     so there's no color flash handing off to the first Flutter frame. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_dark" />
    <item>
        <bitmap android:gravity="center" android:src="@mipmap/launch_logo" />
    </item>
</layer-list>
