{
  "name": "firestore-rules-test",
  "private": true,
  "type": "module",
  "description": "Emulator-backed tests for firestore.rules — run with `npm test` (starts the Firestore emulator, runs run-tests.mjs against it, shuts it down).",
  "scripts": {
    "test": "bash test.sh"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
